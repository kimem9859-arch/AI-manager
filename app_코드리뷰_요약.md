# app.py 코드 리뷰 요약

## 🔴 심각 / 버그

### 1. `st.rerun()` 시 채팅 메시지가 저장되지 않음 (update, notice 성공 시)
- **위치**: 408행, 416행
- **원인**: `st.rerun()`을 호출하면 그 아래 "결과 출력 및 저장" 블록(424~426행)에 도달하지 못함.
- **결과**: 진행률 수정/공지 수정 성공 시 사용자에게 보여줄 "변경했습니다" 메시지가 세션에 남지 않고, 새로고침 후 채팅에 안 보일 수 있음.
- **수정 제안**: `st.rerun()` 직전에 `st.session_state.messages.append(...)` 및 필요 시 `chat_box.chat_message("assistant").write(msg)` 호출하거나, rerun 전에 메시지를 먼저 append 한 뒤 rerun.

### 2. 물품 검색 시 "비고" 컬럼 없으면 에러
- **위치**: 293~296행
- **원인**: `df_display["비고"]`를 사용하는데, "비고" 컬럼이 없을 수 있음.
- **결과**: `KeyError` 발생.
- **수정 제안**: `"비고" in df_display.columns`일 때만 비고 기준 검색에 포함.

### 3. Gemini API 키 미설정 시 에러 메시지 불명확
- **위치**: 129~131행
- **원인**: 로컬에서 `st.secrets`에 `GOOGLE_API_KEY`가 없으면 `genai.configure()`가 호출되지 않음.
- **결과**: 채팅 입력 시 인증 오류로 실패하고, 사용자에게 "API 키를 설정해 주세요" 같은 안내가 없음.
- **수정 제안**: `os.environ.get("GOOGLE_API_KEY")` 또는 설정 파일 fallback 추가 후, 키가 없으면 사이드바 등에 안내 메시지 표시.

---

## 🟠 개선 권장

### 4. bare `except:` 사용
- **위치**: 53~54, 103, 110~111, 114, 122~123, 126, 411~412
- **문제**: 예외 타입을 지정하지 않아 `KeyboardInterrupt` 등까지 삼키고, 원인 파악이 어려움.
- **수정 제안**: `except Exception as e:` 로 바꾸고, 필요 시 `logging` 또는 `st.error`로 로그/표시.

### 5. update 시 `val`이 None일 수 있음
- **위치**: 400~401행
- **문제**: `val = cmd.get("value")`가 None이면 `if "%" not in val`에서 `TypeError`.
- **수정 제안**: `val`이 None이거나 문자열이 아니면 기본값 또는 에러 메시지 처리.

### 6. 추가(add) 시 `row` 검증 없음
- **위치**: 391~394행
- **문제**: `row_vals`가 None이거나 리스트가 아니면 `append_row`에서 예외.
- **수정 제안**: `row_vals`가 리스트인지 확인하고, 아니면 사용자에게 "row 형식이 올바르지 않습니다" 등 메시지 반환.

### 7. 통계와 실제 상태값 불일치 가능
- **위치**: 141~145행
- **문제**: '대기', '진행중', '완료'만 세므로, 오타나 공백('대기 ', ' 진행중')이 있으면 해당 행은 어느 카드에도 안 들어감. 전체 작업 수와 나머지 합이 안 맞을 수 있음.
- **수정 제안**: 상태 값을 strip/정규화하거나, "기타" 항목을 두어 전체 = 대기+진행중+완료+기타가 되도록 처리.

### 8. `ws.find(target)`로 첫 번째 셀만 수정
- **위치**: 404행
- **문제**: 같은 작업명이 여러 행에 있으면 첫 번째 행만 진행률이 수정됨.
- **수정 제안**: 의도가 "첫 번째만"이면 주석으로 명시; 모두 수정하려면 `findall` 후 반복 처리.

### 9. `undo_last_chat()`에서 `time.sleep(0.5)`
- **위치**: 23행
- **문제**: UI 스레드를 블로킹해 잠깐 멈춘 것처럼 보일 수 있음.
- **수정 제안**: `time.sleep` 제거하고 `st.rerun()`만 호출해도 동작 가능.

---

## 🟢 참고 (낮은 우선순위)

- **AI 응답 JSON 추출**: 모델이 ```json ... ``` 밖에 설명을 붙이면 `json.loads` 실패 가능. 중괄호 `{ ... }`만 추출하는 파싱을 추가하면 더 안정적.
- **get_spreadsheet()**: Secrets 실패 시 `service_account.json`도 없으면 예외가 그대로 전파됨. `load_data_safe`처럼 try/except로 한 번 더 감싸고 사용자 메시지/로그를 남기면 좋음.
- **공지 시트 없을 때**: `add_worksheet` 후 "공지없음" 등 초기값을 넣는 로직이 있어, 시트 구조가 달라질 수 있음. 의도된 동작이면 유지.

---

원하시면 위 항목별로 `app.py`에 들어갈 수정 패치(코드 스니펫)도 작성해 드리겠습니다.
